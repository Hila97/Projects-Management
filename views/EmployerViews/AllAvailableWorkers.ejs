<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>book contractor Worker</title>
<!--    <link href="/styles/SearchContractorWorker.css" rel="stylesheet">-->
</head>
<body>
<div>
    <%- include ('../partials/EmployerNav')%>
</div>
<h1>AVAILABLE CONTRACTOR WORKERS</h1>
<div class="table-data">
    <table border="1">
        <tr>
            <th>first Name</th>
            <th>last Name</th>
            <th>field Of Employment</th>
            <th>area Of Residence</th>
            <th>hourly Wage</th>
            <th>rating</th>
            <th>Experience</th>
            <th>book</th>
        </tr>

        <%
        if(workerList.length!=0){
            var i=1;
        workerList.forEach(function(worker){
           // workerId=worker._id
       // console.log(workerId);
        %>
        <tr>
            <td><%= worker.firstName %></td>
            <td><%= worker.lastName %></td>
            <td><%= worker.fieldOfEmployment %></td>
            <td><%= worker.areaOfResidence %></td>
            <td><%= worker.hourlyWage %></td>
            <td><%= worker.rating %></td>
            <td><%= worker.Experience %></td>
            <!--  <td><button class="open-button" onclick="openForm()">BOOK</button>-->

        <td><a class="btn" href="/employment/BookForm/<%=workDate%>/<%=worker._id%>">BOOK</a></td>
        </tr>
        <%  i++; }) %>
        <% } else{ %>
            <tr>
                <td colspan="7">No Data Found</td>
            </tr>
        <% } %>
    </table>
    <!--
    <div class="form-popup" id="myForm">
        <form action="/employment/addEmployment" class="form-container" method="POST">
            <h1>BOOK CONTRACTOR WORKER</h1>
            <h2 id="ttt"></h2>

            <label for="email"><b>Email</b></label>
            <input type="text" placeholder="aaaa" name="email" required>

            <label for="psw"><b>Password</b></label>
            <input type="password" placeholder="Enter Password" name="psw" required>

            <button type="submit" class="btn">CONFIRM BOOKING</button>
            <button type="button" class="btn cancel" onclick="closeForm()">Close</button>
        </form>
    </div>

    <script>
        function openForm(worker) {
            console.log(worker)
            document.getElementById("myForm").style.display = "block";
            //document.getElementById("ttt").innerHTML =worker.firstName;

        }

        function closeForm() {
            document.getElementById("myForm").style.display = "none";
        }
    </script>-->
</div>

</body>
</html>